# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/ad_budget_tracker?schema=public"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server
PORT=3001

# เปิดหลังรัน prisma migrate deploy และ npm run db:backfill-cycles แล้ว
CAMPAIGN_CYCLES_V2_ENABLED=false
